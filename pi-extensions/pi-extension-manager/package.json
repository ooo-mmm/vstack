{
  "name": "pi-extension-manager",
  "version": "0.1.0",
  "description": "Settings-integrated Pi extension inventory, toggles, and schema-driven extension configuration for vstack packages.",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "coding-agent",
    "extensions",
    "settings"
  ],
  "pi": {
    "extensions": [
      "./extensions/extension-manager.ts"
    ]
  },
  "vstack": {
    "extensionManager": {
      "displayName": "Extension Manager",
      "settings": [
        {
          "key": "enabled",
          "label": "Enable manager UI",
          "description": "Expose /extensions and the manager UI. If disabled, /extensions enable remains available as a recovery command.",
          "type": "boolean",
          "default": true,
          "category": "General",
          "apply": "reload",
          "requiresReload": true
        },
        {
          "key": "showBuiltinTools",
          "label": "Show built-in tools",
          "description": "Include Pi built-in tools in the inventory. Disable for an extension-focused list.",
          "type": "boolean",
          "default": false,
          "category": "Inventory",
          "apply": "live"
        },
        {
          "key": "defaultSaveScope",
          "label": "Default save scope",
          "description": "Where manager setting edits are written when the selected item scope is ambiguous.",
          "type": "enum",
          "enumValues": [
            "project",
            "user"
          ],
          "default": "project",
          "category": "Persistence",
          "apply": "live"
        },
        {
          "key": "notifyOnUpdates",
          "label": "Notify on extension updates",
          "description": "At session start, post a one-line notification listing extensions with newer versions available from their vstack source repo or npm registry.",
          "type": "boolean",
          "default": true,
          "category": "Updates",
          "apply": "live"
        }
      ]
    }
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*"
  },
  "files": [
    "assets/",
    "extensions/",
    "README.md",
    "package.json"
  ]
}
